<resources>
    <!-- Base application theme. -->
    <style name="Theme.Session" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>